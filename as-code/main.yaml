jenkins:
  systemMessage: "Welcome to Jenkins"
  numExecutors: 0
  agentProtocols:
    - "JNLP4-connect"
  slaveAgentPort: 50000
  remotingSecurity:
    enabled: true
  securityRealm:
    github:
      githubWebUri: "https://github.com"
      githubApiUri: "https://api.github.com"
      clientID: "${github_client_id}"
      clientSecret: "${github_client_secret}"
      oauthScopes: "read:org,user:email,repo"
  authorizationStrategy:
    globalMatrix:
      grantedPermissions:
        - "Overall/Read:adhocllc"
        - "Job/Read:adhocllc"
        - "View/Read:adhocllc"
        - "Agent/Build:adhocllc"
        - "Overall/Administer:adhocllc*boilerplate2"
  crumbIssuer: "standard"

tool:
  git:
    installations:
      - name: Default
        home: "git"

unclassified:
  location:
    url: ${jenkins_url}
