jenkins:
  systemMessage: "Welcome to Adhocteam Jenkins"
  numExecutors: 0
  agentProtocols:
    - "JNLP4-connect"
  slaveAgentPort: 50000
  remotingSecurity:
    enabled: true
  securityRealm:
    github:
      githubWebUri: "https://github.com"
      githubApiUri: "https://api.github.com"
      clientID: "${github_client_id:-test}"
      clientSecret: "${github_client_secret:-test}"
      oauthScopes: "read:org,user:email,repo"
  authorizationStrategy:
    globalMatrix:
      grantedPermissions:
        - "Overall/Read:adhocteam"
        - "Job/Read:adhocteam*va-demo"
        - "View/Read:adhocteam*va-demo"
        - "Agent/Build:adhocteam*va-demo"
        - "Overall/Administer:adhocteam*va-demo"
  crumbIssuer: "standard"

tool:
  git:
    installations:
      - name: Default
        home: "git"

security:
  remotingCLI:
    enabled: false

unclassified:
  location:
    url: ${jenkins_url:-http://localhost}