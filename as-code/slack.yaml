credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: slack-token
              secret: '${slack_token}'
              description: Slack token


unclassified:
  slackNotifier:
    teamDomain: ${slack_domain}
    tokenCredentialId: slack-token