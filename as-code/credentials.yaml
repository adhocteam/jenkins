credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope:    SYSTEM
              id:       github-user
              username: ${github_user}
              password: ${github_password}
          - string:
              scope: GLOBAL
              id: slack-token
              secret: '${slack_token}'
              description: "Slack token for notifications"